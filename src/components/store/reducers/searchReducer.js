const initialState = {
    products: ['soap', 'powder', 'deodorant', 'paper'], value: 'Products'
};
  
const searchReducer = (state = initialState, action) => {
    return state;
};
  
export default searchReducer;